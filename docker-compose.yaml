version: '3.8'

services:
  internal-proxy:
    image: nginx:alpine
    container_name: internal-proxy
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    network_mode: host
    restart: unless-stopped
